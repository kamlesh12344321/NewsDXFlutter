
import 'package:flutter/material.dart';

class PoweredByAdsWidget extends StatelessWidget {
  const PoweredByAdsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(top: 5, left: 16, right: 16, bottom: 5), child: Container(
      height: 90,
      color: const Color(0xffE1EBFA),
      child: Center(
        child: Row(
          children: [

          ],
        ),
      ) ,
    ),);
  }
}
